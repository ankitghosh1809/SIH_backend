# Starter deps shared by the app shell. Each agent's work order lists what it adds;
# merge those in at stitch time (see the Stitching Plan in any work order file).
fastapi
uvicorn[standard]
pydantic

# Agent C additions
python-multipart   # required for file upload parsing
httpx               # needed by fastapi.testclient.TestClient

# Agent B additions
Pillow
numpy

# Agent A additions
sqlalchemy>=2.0
psycopg2-binary   # Postgres driver for prod (Neon)
pytest

# Agent E additions
reportlab

# Agent M additions — auth + rate limiting
passlib[bcrypt]
bcrypt==4.0.1       # pinned: passlib 1.7.4 (its last release) can't read bcrypt's version
                    # string on bcrypt>=4.1, and with bcrypt 5.x this isn't just a warning —
                    # CryptContext.hash()/.verify() raise outright. 4.0.1 is the newest
                    # version that still works cleanly with passlib. Bump only alongside a
                    # passlib upgrade/fix.
PyJWT               # chosen over python-jose: python-jose is unmaintained since 2021 and has
                    # a real CVE (CVE-2024-33663); FastAPI's own docs now recommend PyJWT.
slowapi
